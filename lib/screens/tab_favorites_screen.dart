import 'package:flutter/material.dart';

class TabFavoritesScreen extends StatelessWidget{
  const TabFavoritesScreen({super.key});

  @override
  Widget build(BuildContext context){
    return const Center(
      child: Text(
        "Favorite Screen",
      ),
    );
  }
}